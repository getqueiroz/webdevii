spring:
  application:
      name: demo
  datasource:
    url: jdbc:h2:mem:demo;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  
  h2:
    console:
      enabled: true
      path: /h2
  
  flyway:
    enabled: true
    locations: classpath:db/migration/h2

springdoc:
  swagger-ui:
    path: /swagger
    

server:
  port: 9090
  
